# 生图模式前端对后端交接

更新时间：2026-08-15 21:39

## 当前前端范围

前端已在 `/chat?tool=image` 接入生图工作区，包含：模型能力读取、提示词、比例、分辨率、质量、参考图入口、生成、取消、轮询、失败重试、结果预览和下载。配置抽屉已增加“对话模型 / 生图模型”标签。

前端不会读取或保存真实 API Key，不会将 base64 结果写入 React state 或 `localStorage`。生图结果只保存在当前页面内存中，刷新后不恢复历史。接口缺失或无配置时显示明确状态，不模拟成功图片。

当前入口约定为 `/chat?tool=image`，不新增 `/image` 路由，也不实现对话自然语言意图识别或 `/image` 命令转入。

## 当前后端接口契约

以下契约以工作区当前后端实现为准，前端已经按此适配。

### GET `/api/ai/image-presets`

需要当前登录用户。无生图配置时返回：

```json
{ "presets": [] }
```

有配置时返回扁平能力字段，不使用嵌套 `capabilities`：

```json
{
  "presets": [
    {
      "id": "user-image-config",
      "label": "我的生图模型",
      "model": "grok-imagine-image",
      "supportsImageGeneration": true,
      "supportsImageEdit": false,
      "aspectRatios": ["1:1", "3:2", "2:3", "4:3", "3:4", "4:5", "5:4", "16:9", "9:16", "2:1", "1:2"],
      "resolutions": ["1k", "2k"],
      "qualities": ["low", "medium", "high"],
      "maxImages": 1
    }
  ]
}
```

前端会兼容扁平或嵌套能力，但实际后端当前返回扁平字段。`maxImages` 当前为 `1`，前端仍会将展示上限限制为 `4`。普通聊天模型不应出现在此列表。

### POST `/api/image-generations`

当前后端使用严格 schema。前端发送：

```json
{
  "requestId": "客户端 UUID",
  "conversationId": "当前会话 UUID",
  "imagePresetId": "user-image-config",
  "prompt": "雨夜东京街道",
  "referenceAttachmentIds": [],
  "aspectRatio": "16:9",
  "resolution": "1k",
  "quality": "high",
  "source": "image-mode"
}
```

`conversationId` 当前为必填 UUID。若用户尚无当前会话，前端会先创建会话再提交生成请求。请求不发送 `count` 字段，因为当前 `imageGenerationSchema` 为 strict schema 且不接受该字段。当前后端也会拒绝非空 `referenceAttachmentIds`，因此 `supportsImageEdit=false` 时前端应保持参考图入口禁用。

响应状态码：

- `202`：任务处于 `queued` 或 `running`，前端随后轮询。
- `200`：任务已进入终态，或服务端返回已存在的幂等结果。

响应结构：

```json
{
  "generation": {
    "id": "generation UUID",
    "status": "queued | running | cancel_requested | completed | failed | cancelled",
    "model": "grok-imagine-image",
    "prompt": "雨夜东京街道",
    "attachments": [
      {
        "id": "attachment UUID",
        "url": "/api/attachments/attachment-uuid",
        "mimeType": "image/webp",
        "width": 1376,
        "height": 768
      }
    ],
    "warnings": []
  }
}
```

生成结果字段为 `attachments`；前端同时兼容旧的 `images` 字段，但后端应以 `attachments` 为准。结果 URL 必须是受保护 URL，不返回 base64、磁盘路径或上游 URL。

### GET `/api/image-generations/:id`

需要当前登录用户，并按用户隔离任务。前端对 `queued`、`running`、`cancel_requested` 每约 1 秒轮询此接口，直到 `completed`、`failed` 或 `cancelled`。任务不存在返回 `404` 和统一错误结构。

### DELETE `/api/image-generations/:id`

需要登录和同源校验。前端点击“停止生成”时调用此接口，然后终止当前轮询。后端返回当前任务状态；无法取消已完成任务时应返回明确状态，不得删除已生成资产。

## 生图配置接口

前端当前调用：

- `GET /api/me/image-config`
- `PUT /api/me/image-config`
- `POST /api/me/image-config/test`
- `DELETE /api/me/image-config`

当前配置 schema：

```json
{
  "name": "我的生图配置",
  "provider": "xai-compatible",
  "baseUrl": "https://api.x.ai/v1",
  "model": "grok-imagine-image",
  "apiKey": "仅首次或更新时传递"
}
```

`provider` 当前只允许 `xai-compatible` 或 `openai-compatible`。`name` 为必填字段。`baseUrl` 必须命中服务端管理员允许列表；首次保存/测试必须填写 API Key，后续留空表示保留已存密钥。

GET 返回的 `config` 只允许包含名称、Provider、Base URL、模型和脱敏状态（如 `apiKeyConfigured`、`apiKeyLast4`），禁止返回明文 API Key。配置保存或删除后，`/api/ai/image-presets` 应立即反映新状态。

## 参考图上传现状

前端入口已保留并复用：

- `POST /api/uploads`
- `DELETE /api/uploads/:id`

但当前生图能力明确返回 `supportsImageEdit=false`，且服务端 schema 拒绝非空 `referenceAttachmentIds`。后端未来启用参考图编辑前，需要同步能力字段、附件归属校验、临时附件生命周期和生图消费规则；在此之前不能把参考图当作可提交能力。

## 后端安全与可靠性要求

- 所有接口复用当前登录鉴权；写操作执行同源校验。
- 以当前用户重新校验配置、预设、会话和附件归属，禁止跨账号读取或生成。
- `requestId` 用于幂等控制，同一用户重复提交不得重复计费。
- 服务端再次校验提示词、能力参数、附件和模型，不信任前端选项。
- API Key 加密存储，日志中不得出现明文密钥、完整上游请求头、base64 或私有磁盘路径。
- 生成、配置测试和上传应具备超时、频率限制、额度/计费审计和可读错误映射。
- 生成成功后先完成资产持久化和数据库状态更新，再清理旧临时资源；取消不得误删已完成资产。
- 账号删除时清理用户生图配置、临时附件和相关任务引用，但保留策略需与产品的历史资产策略一致。

## 统一错误与联调验收

错误响应统一为：

```json
{ "error": "用户可读消息" }
```

建议状态码：`400` 参数/能力不匹配，`401` 未登录，`403` 越权，`404` 配置/任务/附件不存在，`409` 幂等或状态冲突，`413` 文件过大，`429` 频率或额度限制，`502` 上游失败，`504` 上游超时。

联调至少验证：

- 无配置时 presets 为空，前端显示“请先配置生图模型”。
- 配置保存后 presets 返回真实扁平能力，且 `supportsImageEdit` 与服务端能力一致。
- 生成任务按 `queued/running` 返回 202，前端通过 GET 轮询并在终态展示 `attachments`。
- 取消请求、网络中断、重复 `requestId` 不造成重复计费或无限 loading。
- 任务、会话、配置和附件无法跨账号访问。
- API Key、base64、上游原始错误和私有存储路径不会出现在浏览器响应或服务端日志。
