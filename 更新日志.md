# 更新日志

## 2026-08-16

### 多模型配置与控制基础设施

- `user_ai_configs`、`user_image_configs` 升级为多记录 UUID 配置，保留历史密文并兼容旧单条接口和运行时 ID。
- 新增统一 `/api/me/model-configs` CRUD 与已保存配置测试接口，配置按账号隔离并返回脱敏公开字段。
- 新增 MySQL 持久化配置/生图审计事件和账号级固定窗口限流；超限返回 `429` 与 `Retry-After`。
- 生图任务记录图片配置 UUID，修改或删除配置只取消对应未完成任务；worker 完成、失败、取消会写脱敏审计，并定期清理过期记录。

## 2026-08-15

### 模型配置

- 新增每个账号一套用户级 Provider、Base URL、Model 和 API Key 配置。
- 配置抽屉新增“测试连接”：使用当前输入发起一次最小模型请求，成功后再由用户明确保存，不写入配置或聊天历史。
- 自定义预设新增账号级配置名称，名称与 Provider、Base URL、Model 和加密 API Key 一起持久化；YYAPI 默认模型调整为 `grok-4`。
- 新增 `yyapi-grok-01` 固定方案，使用 YYAPI OpenAI Compatible 接口与 `grok-4.5`。
- API Key 使用 `AI_CONFIG_ENCRYPTION_KEY` 和 AES-256-GCM 加密保存，接口只返回脱敏状态和末四位。
- 新增 `/api/me/ai-config` 读取、保存和删除接口；系统预设与用户配置可在聊天工作台中切换。
- 增加 Provider Adapter 注册表、Base URL 安全校验和禁止重定向的模型请求。
- 修改密码或删除账号时清理用户模型凭据。

## 2026-08-15

### 用户模型配置

- 新增用户级模型配置表、AES-256-GCM API Key 加密、环境变量密钥环与自动密钥轮换升级。
- 新增 `GET/PUT/DELETE /api/me/ai-config`，读取仅返回脱敏密钥标识。
- 新增可信中转站 Base URL 白名单，默认允许 `https://www.yyapi.cloud/v1`。
- `/api/ai/presets` 追加“我的配置”预设，`/api/chat` 可使用当前用户的加密配置发起模型请求。
- 配置抽屉改为服务端持久化，移除模型配置的浏览器本地草稿存储。

## 2026-08-14

### 后端与数据

- 增加本地 MySQL 专用应用账号初始化 SQL 和 `.env.example` 连接模板。
- 增加 `users.deleted_at` 与账号软删除流程，原用户名可重新注册，历史聊天和图片数据保留。
- 增加 `PATCH /api/me/password` 修改密码接口；修改密码和软删除都会撤销全部登录会话。
- 过期或停用账号 session 自动清理，API 内部错误不再直接暴露给客户端。

## 2026-08-13

### 新增

- 新增 `一键启动.bat`：双击后自动检查 Node.js 和 npm、按需安装依赖、启动或复用 3000 端口的开发服务，并在页面可访问后自动打开浏览器。

### 修复

- 修复启动脚本打开服务窗口时的工作目录引号解析错误。

## 2026-08-12

### 新增

- 将 `/` 调整为独立的“智能猫娘”展示首页，不再自动跳转登录或聊天页面。
- 新增首页背景图约定：`public/home-backgrounds/` 中的 JPG、JPEG、PNG、WebP、AVIF 文件会自动参与轮播；空目录时显示深色纯色背景。
- 新增首页标题 `🥰智 能 猫 娘😋`、中央 `👍🤓启动🤓👍` 入口、右上角认证状态与退出操作。
- 新增 AI 词汇库与随机浮动气泡，包含大模型、推理、向量、Transformer、Agent、RAG、微调、神经网络和提示词等词汇。
- 新增背景轮播淡入淡出、缓慢左移动画、鼠标接近气泡的排斥交互，以及 `prefers-reduced-motion` 下的静态降级。
- 新增首页内容测试，覆盖支持的背景扩展名、文件名 URL 编码和随机词汇去重。
- 新增本交接文档，记录当前架构、运行配置和维护注意事项。

### 保留与调整

- 右下角 `😋` 外观设置入口继续在首页可用；首页轮播背景优先显示，认证和聊天页继续使用用户自定义背景。
- README 增加了首页背景图目录和支持格式说明。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run lint` 通过。
- `npm run test` 通过，共 4 个测试文件、14 个测试。
- `npm run build` 通过。
## 2026-08-15

- 新增账号头像服务：支持受保护的头像读取、上传、替换和删除接口，头像文件独立于消息附件保存并在账号软删除时清理。
- `users` 增加头像存储键、MIME 类型和更新时间字段，服务端校验真实图片格式、512×512 尺寸和 2 MB 上限。
- 连接方案新增“自定义预设”，可按账号保存 Provider、Base URL、Model 和加密 API Key，并在重新登录后恢复。
- `user_ai_configs` 增加 `preset_id`，区分固定方案、自定义方案与历史兼容记录。
- 模型连接配置改为服务端固定方案目录：支持 OpenAI、Anthropic、Google、xAI、YYAPI、OpenRouter 和 DeepSeek。
- 配置接口只接收方案 ID 与 API Key，账号级加密配置在重新登录后自动恢复；旧的自定义配置保留为只读兼容项。

### 前端体验与页面（补充）

#### 全局外观系统

- 右下角“自定义外观”增加“跟随系统 / 日间 / 夜间”模式；主题色、语义表面、文本、边框、浮层、输入框与浏览器原生控件随模式同步。
- 主题色支持预设与自定义颜色；首页气泡可分别调整颜色差异范围（步长 5）和活跃度（最高 200%）。
- 外观面板改为页面上下文感知：首页显示主题、气泡和背景设置；聊天页仅在开启背景图时显示背景设置；认证及其他页面只显示主题色。
- 聊天页背景图开关改为左侧栏底部的“开启背景图”；背景显示强度和模糊度可保留设置，关闭背景不会删除已上传图片。

涉及文件：`components/appearance-control.tsx`、`lib/appearance.ts`、`app/layout.tsx`、`app/globals.css`、`tests/appearance.test.ts`。

#### 首页与项目介绍

- 首页气泡升级为主题色邻近 RGB 随机色，可保持每个气泡的色彩性格；减少数量与尺寸，保留低干扰漂移、碰撞、鼠标避让和减少动态模式降级。
- 首页扩展为三屏整页滚动：首页主视觉、作者与项目介绍、更多功能占位；支持滚轮单屏平滑切换、触摸滑动、键盘和页码导航。
- 第二屏新增作者简介、GitHub、Bilibili、CSDN 和个人网站入口；头像读取 `/profile/avatar.webp`，加载失败时回退默认图标，并使用主题色星轨头像框。
- 第二屏在夜间显示分层星点、蓝紫星云与左上至右下流星；日间切换为天青、粉色和青色的简约科技轨迹。第三屏同步日夜表面风格。
- 首页“启动”入口对已登录用户进入 `/chat`，未登录用户进入登录页；启动后新增约 600ms 的工作台组装式过渡，聊天页左栏、顶部栏、消息区和输入区依序入场。

涉及文件：`components/home-hero.tsx`、`lib/home-profile.ts`、`lib/home-sections.ts`、`lib/home-sky.ts`、`components/chat-entry-transition.tsx`、`lib/chat-entry-transition.ts`、`app/globals.css`、`tests/home-content.test.ts`、`tests/appearance.test.ts`、`public/profile/avatar.webp`（可选静态头像资源）。

#### 认证与聊天工作台

- 登录和注册成功后统一返回首页；认证页增加“返回首页”，并与首页共用主题色、日夜模式和清透表单视觉。
- `/chat` 重构为可收起左侧工具栏、中央多功能工作区和可展开账号面板；对话、生图、视频、Agent 均具备统一导航，其中对话保留现有会话、流式回复、模型预设和图片输入能力。
- 工作台增加配置抽屉、会话列表、移动端抽屉布局和账号状态占位；再次点击“配置”可关闭配置抽屉。
- 修复聊天背景的 hydration 稳定性和夜间主题融合；输入区与文件上传入口统一为深色主题表面和回形针图标，避免原生“选择文件”控件干扰布局。

涉及文件：`components/auth-form.tsx`、`components/chat-client.tsx`、`app/(chat)/chat/page.tsx`、`app/(auth)/login/page.tsx`、`app/(auth)/register/page.tsx`、`app/globals.css`、`交接文档.md`。

#### 用户头像与个人资料

- 对话消息明确区分左右布局：AI 保持左侧主题色 Bot 图标，用户保持右侧头像与消息气泡。
- 新增受保护的 `/profile` 个人资料页，包含头像预览、手动 1:1 裁切、缩放、拖动和 Canvas WebP 导出流程。
- 头像服务尚未接入时，资料页明确显示“等待服务端头像服务接入”并禁用上传/删除，不将头像写入浏览器本地存储；聊天页继续回退为用户名首字母。
- 新增用户头像的后端交接文档，定义数据库迁移、私有读取接口、上传校验、文件生命周期与账号删除清理要求。

涉及文件：`components/user-avatar.tsx`、`components/profile-client.tsx`、`app/(profile)/profile/page.tsx`、`components/chat-client.tsx`、`lib/user-avatar.ts`、`lib/avatar-crop.ts`、`app/globals.css`、`前后端交接08151458.md`、`tests/avatar.test.ts`。

#### Markdown 对话气泡

- 重构聊天消息行，明确 AI 左侧、用户右侧，用户头像位于右侧消息组最外侧。
- 新增清透主题玻璃气泡，统一日间、夜间、主题色、图片附件和 Markdown 内容的边框、背景、阴影与间距。
- AI 回复继续使用原始 Markdown 文本流，前端边接收边通过 `ReactMarkdown`、GFM 和安全清洗插件渲染。
- AI 首字符到达前显示“思考中…”，流式期间显示“对话生成中…”，完成或错误后结束加载状态。

涉及文件：`components/chat-client.tsx`、`app/globals.css`、`lib/chat-message.ts`、`components/markdown.tsx`、`tests/chat-message.test.ts`。

#### 几何指挥台视觉

- `/chat` 升级为几何指挥台风格，使用细网格、斜线、节点、角标和主题色状态线建立工作台层次。
- 新增克制的鼠标跟随光晕，使用 `requestAnimationFrame` 直接更新 CSS 变量，不触发 React 每帧重渲染；触摸设备与减少动态模式自动停用。
- 精修左侧工具栏、顶部控制带、会话历史、消息工作区、命令输入条、账号面板和配置抽屉，并扩大中央消息区可用宽度。
- 几何环境层适配日间、夜间、自定义主题色和聊天背景图，保持现有业务功能与移动端抽屉行为不变。

涉及文件：`components/chat-ambient-layer.tsx`、`lib/chat-ambient.ts`、`components/chat-client.tsx`、`app/globals.css`、`tests/chat-ambient.test.ts`。

#### 验证

- 覆盖主题派生、页面设置可见性、气泡颜色映射、滚动页码和首页进入聊天的过渡标记等前端单元测试。
- 已执行 `npm run lint`、`npm test`、`npx tsc --noEmit`；生产构建通过。若开发服务器正在运行，Next.js 会占用 `.next/dev/lock`，需停止开发服务后再重复执行 `npm run build`。

涉及文件：`tests/appearance.test.ts`、`tests/home-content.test.ts`、`tests/config.test.ts`、`tests/user-ai-config.test.ts`。

#### 聊天光晕与流式布局修复

- 工作台鼠标光晕增加亮度、动效两个可调滑条，并加入延迟跟随光晕与低频动效；减少动态模式和触摸设备自动停用。
- 聊天工作台固定为视口高度，背景图固定在视口层，消息增长只在消息区域滚动。
- 流式回复改为仅在用户接近底部时跟随，避免查看历史时被拉回；增加回到底部入口并修复会话加载覆盖临时消息导致的错位。
- 修复窗口缩小后左侧栏仍占据 flex 宽度、中央区域出现空白的问题；小屏统一使用覆盖式工具栏抽屉。

涉及文件：`lib/appearance.ts`、`components/appearance-control.tsx`、`components/chat-ambient-layer.tsx`、`lib/chat-ambient.ts`、`components/chat-client.tsx`、`app/globals.css`、`tests/appearance.test.ts`、`tests/chat-ambient.test.ts`。

#### 聊天气泡间距与字体大小

- `/chat` 外观面板新增五档工作台字体大小：极小、小、默认、大、极大，对应 `40% / 80% / 100% / 120% / 160%`。
- 字体偏好兼容旧外观存储并即时同步整个聊天工作台，图标、点击热区和主要布局尺寸保持稳定。
- 统一 AI 与用户气泡的内边距，补充 Markdown 段落、列表、引用、表格和代码块间距，避免文本贴满边框。

涉及文件：`lib/appearance.ts`、`components/appearance-control.tsx`、`app/globals.css`、`tests/appearance.test.ts`。

- 追加修复窄窗口/浏览器缩放下侧栏脱离视觉后仍占据布局宽度的问题：移动端侧栏关闭时完全移出 flex 布局，仅打开时作为覆盖抽屉显示；桌面收起状态固定 `72px` flex 基准。

#### 生图工作区前端

- `/chat?tool=image` 新增生图工作区，支持独立生图预设、提示词、比例、清晰度、质量、数量和参考图能力控制。
- 接入真实生图请求契约，包含生成、取消、错误重试、固定比例骨架、结果网格、预览和下载；接口缺失时明确显示待接入状态，不伪造图片。
- 配置抽屉新增“对话模型 / 生图模型”标签，生图配置独立调用用户级接口，API Key 不写入浏览器存储。
- 新增后端交接文档，明确生图预设、生成、配置、参考图、鉴权、幂等、错误与费用安全要求。
- 根据后端交接补充生图联调：配置保存或删除后立即刷新预设，任务排队超时提示检查 worker，并按脱敏错误码显示可操作文案。
- 修复生图生成、模型加载和参考图上传状态的加载图标不旋转问题，改用独立且可降级的旋转动画。

涉及文件：`components/image-workspace.tsx`、`components/image-composer.tsx`、`components/generated-image-grid.tsx`、`lib/image-generation-client.ts`、`components/chat-client.tsx`、`app/globals.css`、`tests/image-generation-client.test.ts`、`生图模式前端对后端交接08152139.md`、`生图模式后端对前端交接08152143.md`。

#### 工作台“我的配置”

- 工作台配置抽屉新增“配置 / 我的配置”双视图和全部、对话、生图筛选。
- 已保存配置统一展示名称、Provider、模型、Base URL 和仅末四位可见的 API Key 状态，并支持使用、编辑和确认删除。
- 优先适配未来多配置接口；接口未接入时自动回退现有对话、生图单条接口，不伪造多配置能力。
- 固定对话连接方案在兼容模式下保存为带名称的安全连接快照，使自定义名称能够真实绑定账号。
- 新增多配置数据库、CRUD、运行时预设和安全迁移交接文档。

涉及文件：`components/chat-client.tsx`、`components/saved-config-list.tsx`、`components/image-workspace.tsx`、`lib/workspace-configs.ts`、`app/globals.css`、`tests/workspace-configs.test.ts`、`交接08160042.md`。

#### 生图模式后端

- 新增账号级加密生图配置、收费连接测试、独立图片预设、异步生成/查询/取消接口和单并发 MySQL worker。
- 新增 `user_image_configs`、`image_generations` 及 generated attachment 关联；生成结果经过 SSRF、字节、真实格式和像素校验后统一保存为 PNG。
- 使用用户行锁协调入队、配置变更、取消、worker 认领和完成提交，修复取消被完成状态覆盖及排队任务切换连接的竞态。
- 配置读取和模型调用支持旧加密密钥解密及活动密钥升级；配置或账号删除会取消未完成任务，历史成功图片继续保留。
- 增加 Adapter、URL 输出、安全日志、无效图片、幂等、跨用户、取消、轮换和落库测试，并新增精确前端交接文档。
- 生成附件新增可空 `width/height`，worker 保存 Sharp 解码后的最终 PNG 像素尺寸；轮询响应对新图片返回宽高，对历史空值保持兼容。
- 收口前后端交接契约，移除 WebP、prompt、warnings 及应用级 HTTP 429/504 等未实现承诺，并明确首期延期能力。

涉及文件：`lib/image-config.ts`、`lib/ai-image.ts`、`lib/image-generation.ts`、`scripts/image-worker.ts`、`app/api/me/image-config/`、`app/api/image-generations/`、`drizzle/0006_clumsy_blizzard.sql`、`tests/image-generation.test.ts`、`tests/image-generation.integration.test.ts`、`生图模式后端对前端交接08152143.md`。
